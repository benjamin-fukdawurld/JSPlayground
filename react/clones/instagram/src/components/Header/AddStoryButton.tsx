import React from "react";

import { CameraIcon } from "../../icons";

type Props = {};

export default function AddStoryButton({}: Props) {
  return (
    <button>
      <CameraIcon />
    </button>
  );
}
