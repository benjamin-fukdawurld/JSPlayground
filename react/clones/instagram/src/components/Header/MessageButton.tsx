import React from "react";
import { ShareIcon } from "../../icons";

type Props = {};

export default function MessageButton({}: Props) {
  return (
    <button>
      <ShareIcon />
    </button>
  );
}
