import React from "react";
import { MessageIcon } from "../../icons";

type Props = {};

export default function MessageButton({}: Props) {
  return (
    <button>
      <MessageIcon />
    </button>
  );
}
